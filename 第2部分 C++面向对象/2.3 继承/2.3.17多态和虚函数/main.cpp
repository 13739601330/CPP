#include "polymorphism.hpp"

int  main(int argc, const char** argv) 
{



    // animal *p = new dog();
    // p->speak();
    // delete p; //只执行了animal的析构函数

    // dog d; //对象是dog类对象，分配在栈上
   
    // animal *p ;
    // p = &d;
    // p->speak();  //只执行了dog的析构函数

    return 0;
}